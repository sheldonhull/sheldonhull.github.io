{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "serve",
            "type": "shell",
            "command": "powershell.exe -nologo -noprofile -command 'start-process powershell.exe ''-nologo -noprofile -file \"\".\\Serve.ps1\"\" -WorkingDirectory ${WorkspaceDirectory}'''",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "rebuild",
            "type": "shell",
            "command": "powershell.exe -nologo -noprofile -command 'start-process powershell.exe ''-nologo -noprofile -file \"\".\\rebuild.ps1\"\" -WorkingDirectory ${WorkspaceDirectory}'''",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}